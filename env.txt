# Do not put Supabase service_role in VITE_* — it would be exposed in the browser bundle.
# Use VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY (or the values in src/integrations/supabase/client.ts).
# Service role belongs only in Supabase Edge Function secrets or other server-side env.
