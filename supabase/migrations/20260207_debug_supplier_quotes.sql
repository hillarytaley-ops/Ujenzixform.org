-- Migration slot kept for history. Previous revisions contained ad hoc SELECT batches tied
-- to specific accounts; those are not appropriate as automatic migrations.
--
-- Use instead: supabase/scripts/diagnostics/debug-supplier-quotes.sql (manual / SQL Editor).
SELECT 1;
