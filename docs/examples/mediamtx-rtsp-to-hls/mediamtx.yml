# MediaMTX — replace the RTSP source with your camera (VLC-test first).
# HLS: http://<host>:8888/site/index.m3u8 → paste into cameras.stream_url

paths:
  site:
    # Replace with your camera RTSP URL (VLC test first). Optional: add sourceOnDemand, sourceProtocol per MediaMTX docs.
    source: rtsp://USER:PASSWORD@192.168.1.64:554/Streaming/Channels/101
